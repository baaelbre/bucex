#!/usr/bin/env bash
#PBS -N bucex_sim_laplace
#PBS -l walltime=24:00:00
#PBS -l nodes=1:ppn=4
#PBS -l mem=48gb
#PBS -j oe
set -euo pipefail
cd "${PBS_O_WORKDIR}"
mkdir -p logs
CONFIG="${CONFIG:-examples/config/simulation.json}"
exec > "logs/03_simulation_laplace_${PBS_JOBID:-manual}.log" 2>&1
bash bash_scripts/run_03_simulation_laplace.sh "${CONFIG}" "${PBS_NP:-4}"
