# Research

Every active workflow lives in `serra/`. The scripts demonstrate the public
BUCEX API; configuration chooses data, model, priors and run budgets. Start with
[serra/README.md](serra/README.md). Research modules are distributed as source,
while `bucex/` alone is installed as the library package.
